name: platform_video_converter
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/koji-1009/platform_video_converter

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
