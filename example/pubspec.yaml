name: example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter

  platform_video_converter:
    path: ../
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  video_player: ^2.10.1
  cross_file: ^0.3.0
  gal: ^2.3.2
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
